{
  "name": "@izimate/mobile",
  "version": "0.0.1",
  "private": true,
  "main": "index.ts",
  "scripts": {
    "dev": "expo start",
    "build": "echo 'Mobile build handled by EAS'",
    "typecheck": "tsc --noEmit",
    "test": "jest",
    "clean": "rimraf dist .expo"
  },
  "dependencies": {
    "@izimate/shared": "workspace:*",
    "@izimate/api-client": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
