{
  "name": "@izimate/realtime",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "fastify": "^5.3.3",
    "socket.io": "^4.8.0"
  },
  "devDependencies": {
    "tsx": "^4.19.0"
  }
}
