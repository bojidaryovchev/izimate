// infra/eventbridge.ts — EventBridge Scheduler (Phase 6g)
// push-receipts: rate(15 min), cleanup: rate(1 day)

export const eventbridge = {
  pushReceiptsRule: 'placeholder',
  cleanupRule: 'placeholder',
} as any;
